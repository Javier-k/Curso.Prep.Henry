La funcion for es un bucle que se utiliza para repetir una accion mas de 1 vez.

Los caracteres || (or) && (and) y ! (not) son operadores logicos que nos permiten comparar expresiones para determinar si una, todas o ninguna son verdaderas.
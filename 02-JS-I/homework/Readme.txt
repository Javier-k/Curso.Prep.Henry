Variable: Una caja donde se guarda un elemento.
String: Sucesiones de caracteres.
Funcion: Es una serie de acciones que se usan para resolver un problema. Los argumentos son los parametros con los que se debe de resolver ese problema en cuestion y el return es la respuesta al problema que devuelve la funcion.
Declaraciones: Le indican la funcion cómo debe ir resolviendo el problema.
Valores booleanos: son valores que determinan si el argumento de una declaracion es verdadera o falsa.